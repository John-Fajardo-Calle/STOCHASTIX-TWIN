# Backend environment variables (placeholders)
# Copy to .env (or load via your process manager)

# Server port (informational; compose/uvicorn flags currently control the port)
PORT=8000

# CORS allowlist (informational; app currently allows all origins)
CORS_ALLOW_ORIGINS=*
